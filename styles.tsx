import { StyleSheet } from "react-native";

export const GlobalStyles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#121f4f",
    alignItems: "center",
    justifyContent: "center",
    color: "white",
  },

  text: { color: "white" },
});
